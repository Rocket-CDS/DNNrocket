@inherits DNNrocketAPI.render.DNNrocketTokens<Simplisity.SimplisityRazor>
@using DNNrocketAPI;
@using Simplisity;


@AddMetaData("resourcepath", "/DesktopModules/DNNrocket/api/App_LocalResources/")

@{
    var plugin = (SystemRecord)Model.List.First();
}


<div class="w3-container">
    <div class="w3-margin">
        <a class="w3-button w3-teal w3-padding-8 simplisity_click" s-cmd="systemapi_addsetting" s-fields="selecteditemid:@plugin.Info().ItemID,theme:config-w3,template:Admin_SystemDetailSettings.cshtml,ajaxprovider:systemapi" s-return="#settings">@ResourceKey("DNNrocket.cmdAddButton")</a>
        <a class="w3-button w3-pale-yellow w3-padding-8 simplisity_itemundo" s-recylebin="settingsbin" s-return="#pluginsettings" s-undoselector=".settingsdata" style="display: none;">@ResourceKey("DNNrocket.cmdUndoButton")</a>
    </div>

    <div class="w3-row w3-margin">

        <table id="pluginsettings" class="w3-table-all">

            <tr>
                <th class="w3-half">@ResourceKey("system.name")</th>
                <th class="w3-quarter">@ResourceKey("system.value")</th>
                <th style="width:20px;"></th>
            </tr>

            @{
                var lp3 = 0;
            }
            @foreach (var sRecord in plugin.GetSettings())
            {
                var sInfo = new SimplisityInfo(sRecord);
                lp3 += 1;

                <tr class="settingsdata">
                    <td class="w3-half">
                        @TextBox(sInfo, "genxml/textbox/name_" + lp3, "class='w3-input w3-border' maxlength='250' ", "")
                    </td>
                    <td class="w3-quarter">
                        @TextBox(sInfo, "genxml/textbox/value_" + lp3, "class='w3-input w3-border' maxlength='500' ", "")
                    </td>
                    <td style="width:20px;">
                        <a href="javascript:void(0)" class="simplisity_removetablerow" s-recylebin="settingsbin"><i title="Remove" class="fa fa-minus-square fa-fw fa-lg"></i></a>
                    </td>
                </tr>

            }
        </table>

    </div>
</div>
